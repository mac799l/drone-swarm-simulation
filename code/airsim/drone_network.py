import socket

class swarm():
    ip_address = ""

    # Return the gps locations of the other drones in the swarm.
    def gpsSync(pipe):
        gps = pipe.
        results = []
        return
    
    # Return the labels 
    def consensus():
        consensus = False
        label = "NOT_DISASTER"
        # TODO: communicate to other drones for labels.

        print("Consensus reached. Continue.")
        return consensus, label
    
    def vectorSync():
        print("Vectors synced.")
        return
    
    def closeConn():
        print("Connection closed.")
        return